GEMINI_API_KEY=
DATABASE_URL=
FRONTEND_ORIGIN=http://localhost:5173

# Optional outbound email (Gmail SMTP). Leave blank to keep sending disabled.
# GMAIL_APP_PASSWORD is a 16-char Gmail App Password; spaces are ignored.
GMAIL_SENDER=
GMAIL_APP_PASSWORD=
